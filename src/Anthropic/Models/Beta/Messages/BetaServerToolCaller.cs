using System.Collections.Frozen;
using System.Collections.Generic;
using System.Diagnostics.CodeAnalysis;
using System.Text.Json;
using System.Text.Json.Serialization;
using Anthropic.Core;
using Anthropic.Exceptions;

namespace Anthropic.Models.Beta.Messages;

/// <summary>
/// Tool invocation generated by a server-side tool.
/// </summary>
[JsonConverter(typeof(JsonModelConverter<BetaServerToolCaller, BetaServerToolCallerFromRaw>))]
public sealed record class BetaServerToolCaller : JsonModel
{
    public required string ToolID
    {
        get
        {
            this._rawData.Freeze();
            return this._rawData.GetNotNullClass<string>("tool_id");
        }
        init { this._rawData.Set("tool_id", value); }
    }

    public JsonElement Type
    {
        get
        {
            this._rawData.Freeze();
            return this._rawData.GetNotNullStruct<JsonElement>("type");
        }
        init { this._rawData.Set("type", value); }
    }

    /// <inheritdoc/>
    public override void Validate()
    {
        _ = this.ToolID;
        if (
            !JsonElement.DeepEquals(
                this.Type,
                JsonSerializer.SerializeToElement("code_execution_20250825")
            )
        )
        {
            throw new AnthropicInvalidDataException("Invalid value given for constant");
        }
    }

    public BetaServerToolCaller()
    {
        this.Type = JsonSerializer.SerializeToElement("code_execution_20250825");
    }

    public BetaServerToolCaller(BetaServerToolCaller betaServerToolCaller)
        : base(betaServerToolCaller) { }

    public BetaServerToolCaller(IReadOnlyDictionary<string, JsonElement> rawData)
    {
        this._rawData = new(rawData);

        this.Type = JsonSerializer.SerializeToElement("code_execution_20250825");
    }

#pragma warning disable CS8618
    [SetsRequiredMembers]
    BetaServerToolCaller(FrozenDictionary<string, JsonElement> rawData)
    {
        this._rawData = new(rawData);
    }
#pragma warning restore CS8618

    /// <inheritdoc cref="BetaServerToolCallerFromRaw.FromRawUnchecked"/>
    public static BetaServerToolCaller FromRawUnchecked(
        IReadOnlyDictionary<string, JsonElement> rawData
    )
    {
        return new(FrozenDictionary.ToFrozenDictionary(rawData));
    }

    [SetsRequiredMembers]
    public BetaServerToolCaller(string toolID)
        : this()
    {
        this.ToolID = toolID;
    }
}

class BetaServerToolCallerFromRaw : IFromRawJson<BetaServerToolCaller>
{
    /// <inheritdoc/>
    public BetaServerToolCaller FromRawUnchecked(
        IReadOnlyDictionary<string, JsonElement> rawData
    ) => BetaServerToolCaller.FromRawUnchecked(rawData);
}
